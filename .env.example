# Copy this file to ".env" and fill in your token(s).
# The real ".env" file is ignored by git.
#
# Prefer GITHUB_TOKEN.
# GH_TOKEN is an alternate name if you already use GitHub CLI conventions.
#
# Required scope for private repositories is usually: repo

GITHUB_TOKEN=your_github_pat_here
GH_TOKEN=
